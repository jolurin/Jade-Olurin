#Exercise 3
using JLD
using PyPlot
#
pygui(true); using Printf

#1. A

#a.
#loading el-capitan into Julia
imread("el-capitan.png")

#getting Julia to show the image
img = imread("el-capitan.png")
imshow(convert(Array{Float32,3}, img))

#b.
channel = 1
function load_display_extract(img; channel=1)
    img = imread("el-capitan.png")
    imread(img)
    imshow(convert(Array{Float32,3}, img))
    for i =channel:
    Rchannel = img[:,:,1];
    Gchannel = img[:,:,2];
    Bchannel = img[:,:,3];
    return size
    return Rchannel, Gchannel, Bchannel
    println("Dimensions of Rchannel", size(Rchannel))
    println("Dimensions of Gchannel", size(Gchannel))
    println("Dimensions of Bchannel", size(Bchannel))
end
